# 공개 GitHub Pages 배포 중단 (OKR 데이터 보안)
# 로컬 사용: 프로젝트 폴더에서 python3 -m http.server 8080 후 http://localhost:8080
name: Deploy to GitHub Pages
on:
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Public deploy disabled for security."
