# 공개 배포 중단 — 로컬에서만 사용 (python3 -m http.server 8080)
name: Deploy to GitHub Pages
on:
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deploy disabled."
